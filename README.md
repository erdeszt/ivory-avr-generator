# Ivory binding generator for AVR

Generates ivory register and bit field bindings based on the [official avr libc headers](https://www.nongnu.org/avr-libc/).

Only tested with the atmega328p header(included).
